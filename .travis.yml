sudo: required
services: 
  - docker

before_install:
  - docker build -t juniorbezerra01/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true juniorbezerra01/react-test npm test

after_success:
  - docker build -t juniorbezerra01/multi-client ./client
  - docker build -t juniorbezerra01/multi-nginx ./nginx
  - docker build -t juniorbezerra01/multi-server ./server
  - docker build -t juniorbezerra01/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push juniorbezerra01/multi-client
  - docker push juniorbezerra01/multi-nginx
  - docker push juniorbezerra01/multi-server
  - docker push juniorbezerra01/multi-worker

deploy:
  provider: elasticbeanstalk
  region: 'us-east-2'
  app: 'multi-docker'
  env: 'MultiDocker-env'
  bucket_name: 'elasticbeanstalk-us-east-2-861493268032'
  bucket_path: 'docker-multi'
  on:
    branch: main
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY