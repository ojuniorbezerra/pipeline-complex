sudo: required
services: 
  - docker

before_install:
  - docker build -t juniorbezerra/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true juniorbezerra/react-test npm test

after_success:
  - docker build -t juniorbezerra/multi-client ./client
  - docker build -t juniorbezerra/multi-nginx ./nginx
  - docker build -t juniorbezerra/multi-server ./server
  - docker build -t juniorbezerra/multi-worker ./worker
